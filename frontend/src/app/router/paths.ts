import type { RouteKey } from './types';

export const paths: Record<RouteKey, string> = {
  root: '/',
  dashboard: '/dashboard',
  analytics: '/analytics',
  devices: '/devices',
  alerts: '/alerts',
  settings: '/settings',
  reports: '/reports',
};
